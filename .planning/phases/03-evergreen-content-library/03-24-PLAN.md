---
phase: 03
plan: 24
title: Bundle PDF.js worker for Vercel parsing
status: in_progress
---

# Phase 03 Plan 24: Bundle PDF.js worker for Vercel parsing

## Objective

Restore PDF parsing in the deployed admin by ensuring the PDF.js fake-worker
module is initialized and copied into the Vercel server function bundle.

## Context

- @.planning/PROJECT.md
- @.planning/ROADMAP.md
- @.planning/STATE.md
- @.planning/phases/03-evergreen-content-library/03-CONTEXT.md

## Tasks

1. Import the PDF.js worker explicitly after installing the required Node canvas globals.
2. Add a route-scoped Next.js output-file tracing include for the worker module.
3. Add regression coverage for worker initialization and validate the originally rejected PDF locally.

## Verification

- `npm run test:pdf`
- `npm run test:references`
- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- Parse `C:\Saját\gyüli\tanulmányok\A páska tipológiája.pdf` through the production extraction function.
- Confirm the deployed build reaches READY without new PDF worker errors.

## Success Criteria

- PDF.js exposes its worker handler during the extraction regression test.
- The supplied four-page PDF extracts successfully through the application code.
- The production bundle contains the worker dependency and deploys successfully.

---
*Created: 2026-07-31*
